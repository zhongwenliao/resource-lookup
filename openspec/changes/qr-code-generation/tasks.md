# 任务清单：二维码生成（生成端能力规格化）

## 1. 基础模块抽取（共享规则 + 本地存储）

- [x] 1.1 新建 `src/common/qc-code-rules.js`：迁出 `STAGE_CODE_MAP`/`COLOR_CODE_MAP`/`MONTH_CODES`/`normalizeStage`/`normalizeColor`/`pad2`/`pad5` 为导出常量与函数；新增导出 `encodeRecordToBase64url(record, model)`（从 `QcLabel.vue` 的 `encodeRecord` 泛化，model 作入参）与 `splitRuleCode(code)`（规则码从右向左拆 11 段固定段并校验，返回各段对象或 null）。验证：模块可独立 import、无 Vue 组件依赖，`npm run build` 通过
- [x] 1.2 `QcLabel.vue` 删除本地重复定义，改为引用共享模块（模板与交互不动）。验证：同一条 OK 记录在重构前后三种模式码值逐字对比相同；生成页各功能无报错
- [x] 1.3 新建 `src/common/qc-db.js`：IndexedDB 轻封装（数据库 `qc-label-db`、仓库 `import-batches`、`importedAt` 索引），提供写入批次/按导入时间倒序列出/读取/删除接口，打开或写入失败返回错误。验证：浏览器与 Electron 渲染进程下增删查正常，`npm run build` 通过
- [x] 1.4 `QcLabel.vue` 移除明文文本与网页链接模式：删除内容模式切换 UI、展示页地址输入、`encodeRecord` 调用与 url 校验分支，内容固定为编码规则；共享模块的 base64url 编解码导出保留（查询端解析历史码消费）。验证：生成页仅剩编码规则模式且各功能正常，`npm run build` 通过

## 2. 规格逐条核对（行为与规格一致性验证）

- [ ] 2.1 Excel 导入与列识别：上传样例 Excel 核对判定/序号/时间/测量列自动识别、统计卡片、前 5 条预览、判定列手动改选后重新识别、非检测数据与坏文件的错误提示。验证：规格「Excel 导入与列自动识别」4 个场景逐条通过
- [ ] 2.2 编码规则内容模式：11 段拼接与流水递增、码样例实时更新；明文文本与网页链接模式入口已移除不可选。验证：规格「编码规则内容模式」2 个场景逐条通过
- [ ] 2.3 编码规则配置：阶段/颜色填码值原样用、填名称自动转码（V3→3、锖色→Q）、未知输入原样保留；年份/月份自动取当前与手动输入；手动缺码值、手动指定缺日期的校验文案；日期两来源与回退今天；流水码五位补零递增；码样例实时更新。验证：规格「编码规则模式配置」6 个场景逐条通过
- [ ] 2.4 批量生成：进度 0→100 且界面可响应、完成后预览前 12 个并启用打印、单条失败中止并提示序号与原因。验证：规格「批量生成」2 个场景逐条通过
- [ ] 2.5 打印排版：四种标签纸规格（含不干胶纯码）、A4 每页 24 枚带裁切线、三种小票纸连续密集平铺；打印对话框可另存 PDF。验证：规格「标签打印排版」3 个场景在浏览器打印预览中逐条通过
- [ ] 2.6 BarTender 输出：导出 .btxml 内容与码值和浏览器打印一致、模板路径 .btw 校验、浏览器模式一键打印提示降级、Electron 安装包一键调起 BarTender。验证：规格「BarTender BTXML 打印与导出」3 个场景逐条通过
- [x] 2.7 导入数据持久化：解析成功自动入库、判定列改选同步更新当前批次、历史列表按导入时间倒序、载入恢复记录/统计/型号（二维码重新生成）、删除批次、写入失败降级提示。验证：规格「导入数据本地持久化」6 个场景逐条通过
- [x] 2.8 规则码关联持久化：批量生成完成后批次记录明细含每条 OK 记录的规则码、重新生成后同步更新；`qc-db.js` 新增 `findByRuleCode(code)`（遍历批次记录匹配，返回命中记录与所属批次摘要）。验证：规格「规则码与记录关联持久化」2 个场景逐条通过，生成的规则码可检索到对应记录

## 3. 协同与回归

- [x] 3.1 修订 `qr-traceability-lookup` 任务清单：任务 1.1 改为「引用已落地的 `qc-code-rules.js`」、任务 1.2 改为「复用共享模块导出的 `splitRuleCode`/`decodeRecordFromBase64url`」，修正其中 `STAGE_OPTIONS`/`COLOR_OPTIONS` 过时名称，并新增「规则码关联数据查询」任务（查询页规则码结果关联本地批次记录展示，消费 `findByRuleCode`）。验证：该变更任务清单与共享模块实际导出一致，无重复抽取任务，关联查询有对应任务
- [ ] 3.2 端到端回归：上传 Excel → 编码规则生成 → 浏览器打印 / A4 / 小票纸排版、BTXML 导出与改动前一致；刷新页面后从历史批次载入并重新生成成功；`npm run electron:build` 打包成功。验证：同一条记录码值逐字对比相同；安装包内完成一次完整生成、打印与历史批次载入
